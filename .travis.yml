language: python

python:
  - 2.7

install:
  - pip install requests[security]
  - pip install httpie
  - pip install linkchecker

before_script:
  - pyenv versions
  - python --version
  - pip --version
  - pip list
  - linkchecker --version

script:
  - linkchecker --check-extern --timeout 10 ./manual/
